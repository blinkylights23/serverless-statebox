const assert = require('assert')
const serverlessStatebox = require('../index.js')

describe('serverlessStatebox', () => {
  it('has a test', () => {
    assert(false, 'serverlessStatebox should have a test')
  })
})
